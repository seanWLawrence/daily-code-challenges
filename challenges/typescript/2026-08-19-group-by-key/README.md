# Group values by key

Implement `groupBy`. Return an object whose keys are produced by `keyFor`, and whose values preserve the source order of matching items. Do not mutate the input.

```sh
npm test
```

Example: grouping `['ant', 'ape', 'bat']` by first letter returns `{ a: ['ant', 'ape'], b: ['bat'] }`.
