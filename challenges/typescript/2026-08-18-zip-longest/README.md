# Zip arrays to the longest length

Implement `zipLongest`. Pair items from two arrays by index, continuing until the longer input ends. Use `null` whenever one input has no item at that index. Do not mutate either input.

```sh
npm test
```

Example: `zipLongest(["a", "b"], [1])` returns `[["a", 1], ["b", null]]`.
