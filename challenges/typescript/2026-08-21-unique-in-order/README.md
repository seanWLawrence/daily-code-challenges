# Unique values in order

Implement `uniqueInOrder`. Return a new array containing only the first occurrence of each value, preserving source order. Values use JavaScript `Set` equality semantics. Do not mutate the input.

```sh
npm test
```

Example: `uniqueInOrder([3, 1, 3, 2, 1])` returns `[3, 1, 2]`.
