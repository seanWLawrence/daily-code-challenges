# Count words

Implement `countWords`. Return a case-insensitive frequency map of words in text. A word is a consecutive run of letters or digits. Ignore punctuation and whitespace.

```sh
npm test
```

Example: `countWords('Hello, hello! 42')` returns `{ hello: 2, '42': 1 }`.
