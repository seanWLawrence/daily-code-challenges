# Group expense balances

Implement `balances`. A positive result means the person should receive money; a negative result means they owe money. Split every expense evenly among its participants.

Run the fixture from the repository root:

```sh
npm test
```

Bonus: represent money in cents so uneven splits do not introduce floating-point errors.
