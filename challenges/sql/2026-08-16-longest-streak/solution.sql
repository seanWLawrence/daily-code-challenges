-- Replace this placeholder with a query that creates TEMP VIEW answer(user, streak_days, streak_start, streak_end).
CREATE TEMP VIEW answer AS
SELECT NULL AS user, NULL AS streak_days, NULL AS streak_start, NULL AS streak_end WHERE 0;
