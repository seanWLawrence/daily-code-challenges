# Customer order summary

Write a query returning every customer with their order count and total amount. Include customers with no orders, whose total amount should be `0`. Return columns named `customer`, `order_count`, and `total_amount`, sorted by customer.

Your solution must create a temporary view named `answer`.

```sh
npm test
```
