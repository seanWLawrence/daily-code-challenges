-- Replace this placeholder with a query that creates TEMP VIEW answer(customer, order_count, total_amount).
CREATE TEMP VIEW answer AS SELECT NULL AS customer, NULL AS order_count, NULL AS total_amount WHERE 0;
