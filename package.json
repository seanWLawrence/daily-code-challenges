{
  "name": "daily-code-challenges",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "test": "npm run test:typescript && npm run test:sql",
    "test:typescript": "vitest run",
    "test:sql": "node scripts/run-sql-fixtures.mjs",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
